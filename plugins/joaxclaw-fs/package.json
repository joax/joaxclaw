{
  "name": "openclaw-joaxclaw-fs",
  "version": "0.1.0",
  "private": true,
  "description": "Exposes JoaxClaw teams (~/.openclaw/teams) and processes (~/.openclaw/processes) over the gateway as teams.* / processes.* RPC methods, so they're reachable on a remote gateway.",
  "type": "module",
  "main": "./index.js",
  "peerDependencies": { "openclaw": ">=2026.6.0" },
  "peerDependenciesMeta": { "openclaw": { "optional": true } },
  "openclaw": { "extensions": ["./index.js"] }
}
