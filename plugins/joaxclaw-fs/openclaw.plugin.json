{
  "id": "joaxclaw-fs",
  "name": "JoaxClaw FS",
  "description": "Reads and writes JoaxClaw teams and processes in the gateway host's state dir over the WS (teams.* / processes.*), so they work on a remote gateway.",
  "enabledByDefault": true,
  "activation": { "onStartup": true },
  "configSchema": { "type": "object", "additionalProperties": false, "properties": {} }
}
